# Copy this to .env and fill in your values
# DO NOT commit .env to git — only commit .env.sample

# Authentication tokens (populated after login)
AUTH_TOKEN=

# API Keys and secrets (if needed)
API_KEY=

# Other sensitive data
WEBHOOK_SECRET=
